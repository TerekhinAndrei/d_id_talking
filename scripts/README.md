# 🔧 Скрипты управления проектом Talking Head

## 📋 Обзор

Этот раздел содержит удобные скрипты для управления проектом. Все скрипты написаны на Bash и предоставляют цветной вывод с подробной информацией.

## 🚀 Основные скрипты

### Запуск проекта

#### `start-all.sh` - Запуск всего проекта
```bash
./scripts/start-all.sh
```
- ✅ Запускает бэкенд и фронтенд
- ✅ Проверяет зависимости и окружение
- ✅ Показывает статус сервисов
- ✅ Выводит полезную информацию

#### `start-backend.sh` - Запуск только бэкенда
```bash
./scripts/start-backend.sh
```
- ✅ Проверяет Python и зависимости
- ✅ Проверяет переменные окружения
- ✅ Запускает FastAPI сервер
- ✅ Показывает логи запуска

#### `start-frontend.sh` - Запуск только фронтенда
```bash
./scripts/start-frontend.sh
```
- ✅ Проверяет Node.js и npm
- ✅ Устанавливает зависимости если нужно
- ✅ Запускает Vite dev сервер
- ✅ Показывает логи запуска

### Остановка проекта

#### `stop-all.sh` - Остановка всего проекта
```bash
./scripts/stop-all.sh
```
- ✅ Останавливает все сервисы
- ✅ Очищает временные файлы
- ✅ Проверяет статус остановки

#### `stop-backend.sh` - Остановка только бэкенда
```bash
./scripts/stop-backend.sh
```
- ✅ Graceful shutdown процесса
- ✅ Принудительная остановка если нужно
- ✅ Очистка PID файлов

#### `stop-frontend.sh` - Остановка только фронтенда
```bash
./scripts/stop-frontend.sh
```
- ✅ Graceful shutdown процесса
- ✅ Принудительная остановка если нужно
- ✅ Очистка PID файлов

### Перезапуск проекта

#### `restart-all.sh` - Перезапуск всего проекта
```bash
./scripts/restart-all.sh
```
- ✅ Останавливает все сервисы
- ✅ Запускает все сервисы заново
- ✅ Показывает статус

#### `restart-backend.sh` - Перезапуск только бэкенда
```bash
./scripts/restart-backend.sh
```
- ✅ Останавливает бэкенд
- ✅ Запускает бэкенд заново

#### `restart-frontend.sh` - Перезапуск только фронтенда
```bash
./scripts/restart-frontend.sh
```
- ✅ Останавливает фронтенд
- ✅ Запускает фронтенд заново

### Мониторинг

#### `status.sh` - Проверка статуса проекта
```bash
./scripts/status.sh
```
- ✅ Проверяет PID файлы
- ✅ Проверяет занятые порты
- ✅ Проверяет HTTP сервисы
- ✅ Показывает логи и ошибки
- ✅ Проверяет переменные окружения

## 🎯 Быстрый старт

### Первый запуск
```bash
# Запуск всего проекта
./scripts/start-all.sh
```

### Повседневное использование
```bash
# Проверка статуса
./scripts/status.sh

# Перезапуск при изменениях
./scripts/restart-all.sh

# Остановка проекта
./scripts/stop-all.sh
```

## 📊 Возможности скриптов

### ✅ Автоматические проверки
- Проверка корневой папки проекта
- Проверка зависимостей (Python, Node.js, npm)
- Проверка переменных окружения
- Проверка занятых портов

### ✅ Умное управление процессами
- Graceful shutdown с таймаутом
- Принудительная остановка при необходимости
- Управление через PID файлы
- Резервное управление через порты

### ✅ Подробное логирование
- Цветной вывод для разных типов сообщений
- Временные метки
- Сохранение логов в папку `logs/`
- Показ последних ошибок

### ✅ Очистка и обслуживание
- Автоматическая очистка временных файлов
- Удаление кэша Python
- Очистка PID файлов
- Проверка целостности

## 🔧 Конфигурация

### Портты по умолчанию
- **Бэкенд**: 8000
- **Фронтенд**: 5173

### Файлы логов
- **Бэкенд**: `logs/backend.log`
- **Фронтенд**: `logs/frontend.log`

### PID файлы
- **Бэкенд**: `backend.pid`
- **Фронтенд**: `frontend.pid`

## 🚨 Устранение неполадок

### Проблемы с портами
```bash
# Проверка занятых портов
lsof -i :8000
lsof -i :5173

# Принудительная остановка
kill -9 $(lsof -ti :8000)
kill -9 $(lsof -ti :5173)
```

### Проблемы с зависимостями
```bash
# Переустановка Python зависимостей
pip install -r requirements.txt

# Переустановка Node.js зависимостей
cd frontend && npm install
```

### Проблемы с переменными окружения
```bash
# Копирование примера
cp env.example .env

# Редактирование
nano .env
```

## 📝 Примеры использования

### Разработка
```bash
# Запуск для разработки
./scripts/start-all.sh

# Проверка статуса
./scripts/status.sh

# Перезапуск после изменений
./scripts/restart-backend.sh
```

### Тестирование
```bash
# Запуск только бэкенда для тестов
./scripts/start-backend.sh

# Запуск тестов
python -m pytest tests/

# Остановка
./scripts/stop-backend.sh
```

### Продакшен
```bash
# Полная остановка
./scripts/stop-all.sh

# Очистка
rm -rf logs/*.log

# Запуск
./scripts/start-all.sh
```

## 🎉 Преимущества

### Для разработчиков
- ✅ Быстрый запуск проекта
- ✅ Удобное управление сервисами
- ✅ Подробная диагностика
- ✅ Автоматическая очистка

### Для команды
- ✅ Стандартизированные команды
- ✅ Единообразное поведение
- ✅ Подробное логирование
- ✅ Простота использования

### Для продакшена
- ✅ Надежное управление процессами
- ✅ Graceful shutdown
- ✅ Мониторинг состояния
- ✅ Автоматическая очистка

---

*Создано: 16 августа 2025*  
*Автор: AI Assistant*
